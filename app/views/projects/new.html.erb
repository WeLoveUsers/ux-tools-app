<nav class="bc">
  <div class="ui large breadcrumb container">
    <a href="<%= projects_path %>" class="ui right floated inverted button">Annuler</a>
    <a class="section" href="<%= root_path %>"><%= root_path_name %></a>
    <i class="right chevron icon divider"></i>
    <a class="section" href="<%= projects_path %>">Questionnaires UX</a>
    <i class="right chevron icon divider"></i>
    <h1 class="active section">Nouvelle évaluation</h1>
  </div>
</nav>

<%= flash_messages %>

<%= render 'form', project: @project %>
